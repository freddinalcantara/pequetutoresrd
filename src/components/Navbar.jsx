const Navbar = () => {
  return (
    <div className="navbar">
      <input
        type="text"
        placeholder="Buscar libros..."
        className="search-input"
      />
    </div>
  );
};

export default Navbar;
